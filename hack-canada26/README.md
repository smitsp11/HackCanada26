# FixLens Frontend (Next.js)

This folder is frontend-only.

## Run

```bash
cd hack-canada26
npm install
npm run dev
```

Frontend runs on `http://localhost:3000`.

## Backend

Backend is separated into `../backend`.
Run it independently on `http://localhost:8000`.
